<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:OrquestadorFrontend.Features.Notifications"
             xmlns:shared="clr-namespace:OrquestadorFrontend.Shared.Controls"
             x:Class="OrquestadorFrontend.Features.Notifications.NotificationPreferencesPage"
             x:DataType="vm:NotificationPreferencesViewModel"
             Title="Preferencias"
             Shell.TabBarIsVisible="False">

    <Grid Padding="16">
        <shared:LoadingState IsVisible="{Binding IsLoading}" />

        <CollectionView IsVisible="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"
                        ItemsSource="{Binding Preferences}"
                        SelectionMode="None">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="8" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="vm:PreferenceItem">
                    <Border Style="{StaticResource CardBorder}">
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                            <Label Grid.Column="0"
                                   Text="{Binding Label}"
                                   Style="{StaticResource BodyMedium}"
                                   VerticalOptions="Center" />
                            <Switch Grid.Column="1"
                                    IsToggled="{Binding Enabled, Mode=TwoWay}"
                                    VerticalOptions="Center" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>

</ContentPage>
